# Scratch — 002: Core Data Model & TypeScript Types

## 2026-02-23 — Story created

Story created from Phase 1 design doc. This story runs in parallel with 001 — they share `src/types/schema.ts`. The key output is the shared type vocabulary that every other story imports.

Note: stories 001 and 002 have a bidirectional dependency on schema types. In practice, start with the types in 002 and have 001 import them.
